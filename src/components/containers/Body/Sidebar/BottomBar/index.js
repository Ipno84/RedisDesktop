import Button from "./Button";
import React from "react";
import Styled from "./styled";

const BottomBar = () => {
    return (
        <Styled>
            <Button>+</Button>
            <Button>-</Button>
        </Styled>
    );
};

export default BottomBar;
