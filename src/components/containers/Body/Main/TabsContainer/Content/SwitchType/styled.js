import styled from "styled-components";

export default styled.div`
    height: 0.7rem;
    position: relative;
    z-index: 2;
    & > div {
        display: flex;
        flex-direction: column;
        height: 0.7rem;
    }
`;
