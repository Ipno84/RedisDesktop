import ClientFormContainer from "./ClientFormContainer";
import React from "react";
import Styled from "./styled";

const Main = () => {
    return (
        <Styled>
            <ClientFormContainer />
        </Styled>
    );
};

export default Main;
